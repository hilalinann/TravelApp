import SwiftUI

struct Theme {
    static let backgroundColor = Color(uiColor: .systemBackground)
    static let secondaryBackgroundColor = Color(uiColor: .secondarySystemBackground)
    static let textColor = Color(uiColor: .label)
    static let secondaryTextColor = Color(uiColor: .secondaryLabel)
    static let accentColor = Color.blue
    static let favoriteColor = Color.red
    static let shadowColor = Color.black.opacity(0.1)
    
    static let cardBackgroundColor = Color(uiColor: .secondarySystemBackground)
    static let navigationBarBackgroundColor = Color(uiColor: .systemBackground).opacity(0.8)
    
    static let mapPinBackgroundColor = Color(uiColor: .systemBackground)
    static let mapPinShadowColor = Color.black.opacity(0.1)
} 